# Secrets — .env holds real Slack user tokens.
# Broad match so editor swap/backup files (.env.swp, .env~, .env.bak) can never be committed.
.env
.env.*
*.swp
*.swo
*~
!.env.example

node_modules/
dist/

# Runtime state
data.db
data.db-journal
data.db-wal
data.db-shm

.DS_Store
